databases:
  - name: race-db
    databaseName: race_app
    user: race_user
    plan: free

services:
  - type: web
    name: race-candidates-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /healthz
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
      - key: DATABASE_URL
        fromDatabase:
          name: race-db
          property: connectionString
      - key: EXTERNAL_URL
        sync: false